"use strict";
{
    // const rollNumbers: number[] = [25, 45, 45];
    const rollNumbers = [25, 45, 45];
    // const mentors: string[] = ["taz", 'raj', 'jim'];
    const mentors = ["taz", 'raj', 'jim'];
    const boolArray = [true, false, true];
    const user = [
        {
            name: "mezba",
            age: 100
        },
        {
            name: 'jankar',
            age: "110"
        }
    ];
    // generic tuple
    const manush = ["mr.x", "mr.y"];
    const add = (x, y) => x + y;
    add(30, 20);
}
